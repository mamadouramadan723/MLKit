import 'package:flutter/material.dart';

class FaceDetectionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Face Detection'),
      ),
      body: Center(
        child: Text('Face Detection Page'),
      ),
    );
  }
}
